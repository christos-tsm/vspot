import type {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Σύνδεση | vSpot - All about Volos',
    description: '...',
}
const LoginPage = () => {
    return (
        <div>

        </div>
    );
};

export default LoginPage;